class Rect {
  constructor({ x, y, height, width, fillStyle }) {
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
    this.fillStyle = fillStyle;
    this.type = "Rect";
  }
}
